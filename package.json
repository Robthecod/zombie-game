{
  "name": "zombie-chase",
  "version": "1.0.0",
  "description": "Zombie chase - survive on the map",
  "private": true,
  "scripts": {
    "client": "cd client && npm start",
    "build:apk": "cd client && npm run build && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {}
}
