# Optional — the game client is fully offline (no server needed).
# Set this only if you wire up the multiplayer server and restart the app after changing it.
# REACT_APP_SOCKET_URL=https://your-deployed-server.example.com
